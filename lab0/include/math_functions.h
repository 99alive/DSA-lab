#ifndef MATH_FUNCTIONS_H
#define MATH_FUNCTIONS_H

// Returns the square root of `number`.
// Throws std::domain_error if `number` is negative.
double compute_square_root(double number);

#endif // MATH_FUNCTIONS_H
